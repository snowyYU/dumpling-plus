import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import DpCheckboxGroup from "~/checkbox-group";
import { ref } from "vue";
import { props, options } from './example/config.ts'

<Meta title="checkbox-group 多选框组" component={DpCheckboxGroup} argTypes={{
  // props
  "model-value / v-model": {
    table: {
      type: {
        summary: 'array',
      },
      defaultValue: {
        summary: '[]',
      },
      category: 'props'
    },
    description: "绑定值",
  },
  props: {
    description: "多选框label和value对应key配置 ",
  },
  options: {
    description: "多选框配置列表",
  },
  // emits
  "blur": {
    table: {
      category: 'event'
    },
    description: "",
  }, 
  "focus": {
    table: {
      category: 'event'
    },
    description: "",
  },
  "change": {
    table: {
      category: 'event'
    },
    description: "当绑定值变化时触发的事件",
  },
  "input": {
    table: {
      category: 'event'
    },
    description: "",
  },
  "clear": {
    table: {
      category: 'event'
    },
    description: "",
  },
  "select": {
    table: {
      category: 'event'
    },
    description: "",
  }
}}/>

# checkbox-group 多选框
可通过props对象自定义option列表对应的label和value的key值

export const Template = (args) => ({
  components: { 
    DpCheckboxGroup
  },
  setup() {
    const checkList = ref([])
    return {
      checkList,
      ...args
    }
  },
  template: `
    <dp-checkbox-group v-model="checkList" :props="props" :options="options">
    </dp-checkbox-group>
  `,
})


<Canvas>
  <Story
    name="Base"
    args={{
      props: props,
      options: options,
    }}
  >
  {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Base" />