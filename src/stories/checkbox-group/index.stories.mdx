import { Canvas, Meta, Story, ArgsTable, PRIMARY_STORY } from '@storybook/addon-docs';
import DpCheckboxGroup from "~/checkbox-group";
import { ref } from "vue";
import { argTypes } from './stories.config'
import { props, options } from './example/config.ts'


<Meta title="DpCheckboxGroup 多选框组" component={DpCheckboxGroup} argTypes={argTypes}/>

# checkbox-group 多选框
可通过props对象自定义option列表对应的label和value的key值

export const Template = (args) => ({
  components: { 
    DpCheckboxGroup
  },
  setup() {
    const checkList = ref([])
    return {
      checkList,
      ...args
    }
  },
  template: `
    <dp-checkbox-group v-model="checkList" :props="props" :options="options">
    </dp-checkbox-group>
  `,
})


<Canvas>
  <Story
    name="Base"
    args={{
      props: props,
      options: options,
    }}
  >
  {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story={PRIMARY_STORY}/>